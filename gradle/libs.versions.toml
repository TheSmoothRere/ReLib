[versions]
loomVersion="1.17-SNAPSHOT"
loaderVersion="0.19.3"
mcModPublisher="1.0.0"

[plugins]
fabric-loom = { id = "net.fabricmc.fabric-loom", version.ref = "loomVersion" }
mc-mod-publisher = {id = "io.github.smootheez.mc-mod-publisher", version.ref = "mcModPublisher" }

[libraries]
fabric-loader = { group = "net.fabricmc", name = "fabric-loader", version.ref = "loaderVersion" }
